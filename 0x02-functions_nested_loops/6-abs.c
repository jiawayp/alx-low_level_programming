#include "main.h"

/**
 * _abs - calculate the absolute value
 *
 * @n: is an integer
 *
 * Return: return int
 */

int _abs(int n)
{
	if (n == 0)
	{
		return (-);
	}
	else if (n > 1)
	{
		return (n);
	}
	else
	{
		return (n);
	}
}
